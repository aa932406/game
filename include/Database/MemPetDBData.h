#pragma once

#include <cstdint>
#include <string>
#include <vector>
#include <map>
#include <list>
#include <set>
#include <cstring>

class MemPetDBData : public IDataStruct {
public:
    MemPetDBData();
    ~MemPetDBData();
    void CleanUp(MemPetDBData *const this);

private:
    // TODO: member variables
};
（内容由AI生成，仅供参考）
